services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webapp
    restart: unless-stopped
    ports:
      - "90:80"
    networks:
      - devops_net

networks:
  devops_net:
    name: devops_api_node_network
    external: true